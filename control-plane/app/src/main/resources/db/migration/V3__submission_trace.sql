-- 학습용 실행 트레이스 (기술 설계서 §7).
--
-- 판정과 독립이다. 트레이스가 없거나 실패해도 제출의 판정은 그대로 유효하다 (§12.2).
-- 슬라이스는 요약 트레이스를 이 컬럼에 담는다. 운영에서는 manifest 와 청크를 오브젝트
-- 스토어에 두고 참조만 보관해야 한다 (§7.4 실행당 20MB hard cap).

ALTER TABLE submission ADD COLUMN trace JSONB;
