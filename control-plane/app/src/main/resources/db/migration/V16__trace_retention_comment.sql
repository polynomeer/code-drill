-- V5 가 남긴 메모를 고친다.
--
-- V5 에는 "정리 작업이 붙기 전까지는 수동 삭제 대상이다"라고 적혀 있었고, TraceRetention
-- 이 붙으면서 그 문장이 거짓이 됐다. **이미 적용된 마이그레이션 파일을 고쳐서는 안 된다**
-- — Flyway 는 체크섬으로 그것을 잡고, 잡는 순간 앱이 뜨지 않는다. 지난 문장은 그때의
-- 사실이므로 그대로 두고, 지금의 사실은 새 마이그레이션으로 적는다.

COMMENT ON TABLE trace IS
    '원본 트레이스. 보존 기간은 30일이며 TraceRetention 이 주기적으로 지운다 (§7.4).';
