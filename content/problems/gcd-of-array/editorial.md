# 해설

## 관찰

여럿의 최대공약수는 둘씩 접어 나가면 된다 — gcd(a, b, c) = gcd(gcd(a, b), c).

## 접근

0 에서 시작해 원소마다 절댓값과의 gcd 를 취한다. gcd(0, x) = x 라 시작값이 자연스럽고, 모두 0 이면 0 이 그대로 남는다.

## 복잡도

O(n log M) 시간, O(1) 공간.

## 발전 과정

약수를 하나씩 시험하는 방법 → 유클리드 호제법으로 접기.

## 흔히 무너지는 곳

음수를 절댓값으로 바꾸지 않는 것. 모두 0 인 경우.
