# 해설

## 관찰

시계 방향 90도에서 (r, c) 는 (c, rows-1-r) 로 간다. 결과의 모양이 cols × rows 로 바뀐다.

## 접근

결과 격자를 cols × rows 로 만들고 위 공식으로 옮겨 담는다.

## 복잡도

O(rows × cols) 시간, O(rows × cols) 공간.

## 발전 과정

전치 뒤 각 행 뒤집기로도 같은 결과다. 정사각형이 아니면 제자리 회전은 불가능하다.

## 흔히 무너지는 곳

결과의 행·열 크기를 바꾸지 않는 것. 정사각형으로만 시험해 이 실수를 못 보는 것.
