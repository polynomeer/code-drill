# 해설

## 관찰

`out[i] = out[i-1] + nums[i]`. 누적 합의 정의 자체가 점화식이다.

## 접근

합을 하나 들고 앞에서부터 더하며 적는다.

## 복잡도

O(n). 매번 처음부터 더하면 O(n²) 이고 20 만 개에서 잡힌다.

## 발전 과정

이것이 누적 합 배열이고, 구간 합 질의(range-sum-queries)의 재료다.

## 흔히 무너지는 곳

첫 원소를 빼먹는 것. 자기 앞까지의 합을 적어 한 칸 밀리는 것. 매번 다시 더하는 것.
