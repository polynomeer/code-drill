# 해설

## 관찰

결과의 (i + k) % n 자리에 원래 i 번째 값이 온다. k 가 n 보다 클 수 있으니 먼저 n 으로 나눈 나머지로 만든다.

## 접근

새 배열을 만들어 자리를 옮긴다. 제자리에서 하려면 전체를 뒤집고 앞 k 개와 나머지를 각각 다시 뒤집는다.

## 복잡도

O(n) 시간, O(n) 또는 O(1) 공간.

## 발전 과정

한 칸씩 k 번 회전하는 O(n·k) → 인덱스 계산 O(n).

## 흔히 무너지는 곳

k ≥ n 을 처리하지 않는 것. k = 0 이나 n 일 때 그대로여야 한다.
