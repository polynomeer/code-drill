# 해설

## 관찰

창 안의 0 이 k 개를 넘지 않으면 그 창은 통째로 1 로 만들 수 있다. 창을 늘리다 0 이 k 를 넘으면 왼쪽을 줄인다.

## 접근

오른쪽을 한 칸씩 늘리며 0 의 수를 세고, k 를 넘으면 왼쪽을 0 이 하나 빠질 때까지 민다. 창의 최대 길이가 답이다.

## 복잡도

O(n) 시간, O(1) 공간.

## 발전 과정

모든 구간 O(n²) → 슬라이딩 윈도우 O(n).

## 흔히 무너지는 곳

k = 0 일 때, 그리고 전부 1 인 입력. 왼쪽을 줄일 때 0 의 수를 함께 줄이지 않는 것.
