# 해설

## 관찰

같은 값을 두 번 XOR 하면 0 이다. 정확히 두 번 나온다는 조건 덕에 전부 XOR 하면 짝지은 것들이 지워지고 홀로 남은 값만 남는다.

## 접근

0 에서 시작해 모든 원소를 XOR 한다.

## 복잡도

O(n) 시간, O(1) 공간.

## 발전 과정

정렬 뒤 이웃 비교 O(n log n) → 해시맵 세기 O(n) 공간 → XOR O(1) 공간.

## 흔히 무너지는 곳

XOR 대신 합으로 접근하는 것 — 합은 짝을 지우지 못한다.
