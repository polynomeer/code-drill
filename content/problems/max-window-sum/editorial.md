# 해설

## 관찰

창을 한 칸 옮기면 합에서 바뀌는 것은 둘뿐이다 — 나가는 원소와 들어오는 원소.

## 접근

첫 k 개의 합을 만들고, 그 뒤로 한 칸씩 밀며 나가는 것을 빼고 들어오는 것을 더한다. 최댓값을 갱신한다.

## 복잡도

O(n) 시간, O(1) 공간.

## 발전 과정

창마다 다시 더하는 O(n·k) → 슬라이딩 윈도우 O(n).

## 흔히 무너지는 곳

k 가 배열 길이와 같을 때, 그리고 첫 창의 합을 최댓값 초기값으로 쓰지 않는 것.
